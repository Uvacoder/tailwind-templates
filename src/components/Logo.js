export function Logo({ className, ...props }) {
  return (
    <h2 className="text-2xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-teal-500 to-blue-500">
      ( Open Templates )
    </h2>
  )
}
